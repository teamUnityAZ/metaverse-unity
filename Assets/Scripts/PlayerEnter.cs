using UnityEngine;

public class PlayerEnter : MonoBehaviour
{
    // Start is called before the first frame update
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
        
    //}

    //public override void OnPlayerEnteredRoom(Player newPlayer)
    //{
    //    //base.OnPlayerEnteredRoom(newPlayer);
    //    Debug.Log("Call hua enter==="+newPlayer.IsMasterClient);

    //    GameObject myobj = GameObject.FindGameObjectWithTag("PhotonLocalPlayer");
    //    Debug.Log("object for is mine " + myobj.GetComponent<PhotonView>().IsMine + "view id object==" + myobj.GetComponent<PhotonView>().ViewID);
    //    //myobj.GetComponent<RPCCallforBufferPlayers>().sendData();
    //    Debug.Log("send data player enter");
    //}
}
